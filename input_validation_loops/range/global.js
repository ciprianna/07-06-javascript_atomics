var NumberChoice = prompt("Choose a number between 1 and 8", "");

for (NumberChoice; (NumberChoice >= 1 && NumberChoice <= 8); NumberChoice = prompt("Choose a number between 1 and 8", ""));

alert("Goodbye!");
